# Caixa de entrada de ideias

Arquivos criados pelo comando `/ideia`: `AAAA-MM-DD-slug.md`, com frontmatter
`status: nova | roteada | descartada`. Ideia roteada aponta os IDs das tarefas que
gerou. O `/status` reporta ideias paradas em `nova`.
