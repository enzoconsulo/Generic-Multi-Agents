# 2026-08-01 — /trabalhar banco-imobiliario (rodada autônoma)

## Preparação / saneamento

- Sem tarefas `em-execucao` órfãs (nenhum saneamento de retorno a `pronta` necessário).
- **Divergência corrigida:** T-012 tinha a seção `## Verificação` inteiramente preenchida
  (8/8 critérios PASSOU, `npm test` 155/155, "Nenhuma reprovação") mas o frontmatter ainda
  marcava `status: em-teste` — mesma classe do incidente 30/07 (job cortado no meio antes
  de gravar o status final). Corrigido para `status: em-revisao` sem redespachar o
  testador (o relatório já está completo e não é re-executado).
- Promovida `T-015` `backlog → pronta` (dependências T-010 e T-011 `concluida`).
- Estado inicial do backlog: 11 `concluida` (T-001..T-005, T-007..T-011, T-013),
  `em-revisao`: T-006, T-012; `em-teste`: T-014; `pronta`: T-015, T-016; `backlog`:
  T-017..T-022 (dependências pendentes).

## Medição de custo dos agentes refinados (jobs fd99c17b e 7a1f9a45)

Rodadas de `/trabalhar banco-imobiliario` pelo painel (headless, `sonnet`), disparadas
para medir o efeito do refino dos agentes (commit `9e2c9c6`) contra a linha de base dos
jobs `c6d8cede` e `5d8fca81`.

| Job | Mistura de etapas | Desp. | Ferr/desp. | US$ est. | US$/desp. |
|---|---|---|---|---|---|
| `c6d8cede` (base) | construção | 12 | 18,2 | 5,86 | 0,49 |
| `5d8fca81` (base) | construção + T-006 de 5 `areas` | 10 | 25,8 | 6,55 | 0,66 |
| `fd99c17b` (novo) | só portões | 6 | 16,8 | 2,81 | 0,47 |
| `7a1f9a45` (novo) | construção + retrabalho + replanejamento | 11 | 22,3 | 7,45 | 0,68 |

**Veredito: não há melhora de custo mensurável.** Agregado: 21,6 → 20,4 ferr/despacho
(−6%) e US$ 0,56 → US$ 0,60 por despacho (+7%) — os dois dentro do ruído. O único sinal
consistente é o `revisor` (16,0/16,7 → 13,3/15,7), que foi o agente com a mudança mais
concreta (leitura de abertura em paralelo + caminho pelo hash).

**A medição está confundida por desenho:** cada janela roda TAREFAS DIFERENTES, então o
que varia entre jobs é dificuldade de tarefa tanto quanto qualidade de prompt. Custo por
tarefa é ainda pior como comparador — depende da mistura de etapas (a `fd99c17b` fechou 3
tarefas sem construir nada). Para resposta de verdade: A/B na MESMA entrada, que é o que
`painel/integracao/medir-esforco.ts` já faz (restaura o repositório entre as pernas).

**Ganhos verificáveis que não são custo:**
- A T-017 nasceu com 5 `areas` e foi QUEBRADA em T-017a/b/c pelo planejador antes do
  despacho — a regra de tamanho medido funcionando na direção certa (T-017 `cancelada`).
- Disciplina do hash cumprida em T-015 e T-016 (commit de entrega + commit do hash). Em
  31/07, 3 das 5 tarefas ficaram sem o hash.
- Retrabalho da T-016 (revisor achou dinheiro negativo e posições duplicadas na troca)
  custou 12 ferramentas no `engine-reforcado` — coerente com a regra nova de que o
  orçamento do retrabalho é MENOR que o da primeira execução.

**Falhas da rodada:**
- `maxTurns: 120` NÃO impediu o estouro de cota (211 turnos somados). Instrumento errado:
  `num_turns` é somado entre os `result`, inclusive os dos subagentes, e `maxTurns` limita
  só o laço do orquestrador. Não existe hoje teto de custo por job.
- O `testador` deixou `teste-socket-troca.mjs` na árvore, violando a regra de apagar
  auxiliares. Removido pelo orquestrador.

Estado ao fim: 16 `concluida`, 1 `cancelada` (T-017), 1 `pronta` (T-017a), 7 `backlog`.
Cota reabre 17:30 BRT.
