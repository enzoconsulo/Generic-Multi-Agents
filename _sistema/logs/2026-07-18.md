# Log — 2026-07-18

## Resumo
Fábrica criada e configurada do zero: constituição do orquestrador (CLAUDE.md), 6
agentes (planejador, executor, testador, revisor, documentador, pesquisador — todos
`model: inherit`), 5 comandos (/novo-projeto, /ideia, /trabalhar, /status, /encerrar-dia),
protocolo de tarefas baseado em arquivos e templates. Nenhum projeto criado ainda.

## Decisões
- Estado do sistema vive em arquivos (frontmatter das tarefas = fonte única de verdade),
  para sobreviver ao fim das sessões.
- Cada projeto será um repositório git independente; a raiz da fábrica não é repositório.
- Agentes com `model: inherit`: seguem o modelo da sessão principal (preferência do
  usuário: Fable enquanto tiver acesso; Opus como alternativa depois).

## Segunda rodada (mesmo dia): otimização para autossuficiência
- Allowlist de permissões em `.claude/settings.json` (git, npm, node, python, pytest,
  go, cargo, dotnet...) — a fábrica roda sem parar para pedir aprovação dos comandos comuns.
- Novo comando `/manutencao`: autodiagnóstico semanal (valida frontmatters, dependências,
  git limpo, docs; corrige o mecânico, reporta o resto).
- CLAUDE.md: seções "Disciplina de contexto" (orquestrador escaneia frontmatters via
  busca, nunca lê código de projeto) e "Modelo de despacho" (esqueleto padrão de prompt
  para agentes).
- Ajustes nos agentes: executor com regra de desempenho (testes direcionados durante,
  suíte completa 1x no final); testador com timebox de setup (~15 min; ambiente que não
  sobe = reprovação).
- README reescrito como manual completo de operação (9 seções: rotina, receitas,
  intervenções manuais, extensão, ajustes finos, troubleshooting, mapa de arquivos).

## Terceira rodada (mesmo dia): reanálise da arquitetura e refinamento geral
- **Raiz da fábrica virou repositório git de verdade** — o CLAUDE.md prometia, mas o
  `git init` nunca tinha sido rodado. Criado `.gitignore` (projetos/, settings.local.json).
- **Paralelismo refeito sobre a realidade de árvore única por projeto:** a suíte completa
  roda 1x por ciclo (no testador, nunca no executor — antes rodava nos dois, dobrando o
  custo), testador exige projeto quieto (sem executor/testador ativo no mesmo projeto),
  revisor pode sobrepor (lê diff commitado). Elimina reprovação falsa por interferência.
- **Marco de fase agora persiste:** linha `Marco:` por fase no PLANO.md (template +
  planejador + /trabalhar + /status). Antes, nada registrava que um marco já tinha
  rodado — sessão nova poderia repeti-lo ou pulá-lo.
- **Saneamento corrigido:** `em-teste`/`em-revisao` não regridem mais para `pronta` (o
  trabalho anterior está commitado/registrado; regressão jogava fora um ciclo de
  executor); só `em-execucao` regride.
- **Protocolo:** transições que faltavam na tabela (pulo de teste, saneamento),
  `Ciclo N` = valor de `tentativas`, e falha de suíte pré-existente/alheia ao escopo não
  reprova tarefa (vira nota + tarefa corretiva separada).
- **Atalhos de gestão:** orquestrador cria tarefa corretiva pontual direto pelo template
  (1 causa raiz óbvia); /ideia trivial não despacha mais planejador; /ideia e
  /encerrar-dia agora commitam gestão (antes /encerrar-dia violava a regra do CLAUDE.md).
- **Allowlist ampliada** com paridade Bash/PowerShell: uv/uvx, curl,
  Invoke-WebRequest/RestMethod, cmdlets comuns (New-Item, Copy-Item, Test-Path...) —
  menos paradas por prompt de permissão no meio do pipeline.
- **Consertos:** ARQUITETURA dizia `model: opus` no guia de extensão (agora `inherit`) e
  o diagrama não listava /manutencao; testador agora apaga arquivos auxiliares ao final.

## Amanhã
- Primeiro uso real: `/novo-projeto <nome> — <descrição>` com a primeira ideia do
  usuário, depois `/trabalhar`.
