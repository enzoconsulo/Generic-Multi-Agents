# Log — 2026-07-27

## Resumo
Dia em que o **painel-fabrica ficou pronto**: as 20 tarefas concluídas, as 3 fases com
marco registrado, e a última pendência técnica do projeto (`canUseTool`) validada em
execução paga. Também foi o dia em que o repositório da fábrica passou a ser clonável de
verdade (GitHub) e ganhou um script de partida único.

## Feito

### Repositório publicado e tornado clonável
- Restaurado o `.gitignore` da raiz (estava com TODAS as regras comentadas — não ignorava
  nada) e removido do README um caminho de clone fixo que não existia nem nesta máquina.
- Repositório publicado em `github.com/enzoconsulo/Generic-Multi-Agents`.
- Criados `iniciar.ps1` + `INICIAR.bat` na raiz: conferem pré-requisitos (Node 22+, npm,
  git, login do Claude), criam `projetos/` (que fica fora do git e some no clone), fazem
  `npm install`/`build` só quando falta, liberam a porta e abrem o navegador quando o
  servidor responde de verdade. Testado ponta a ponta.

### Painel — Fase 2 fechada, Fase 3 inteira
- **T-016** ações por projeto na UI (trabalhar/status, lock ao vivo, refetch sem F5).
  Descoberto que ela era a última tarefa da **Fase 2** (não da 3, como a instrução de
  retomada presumia) — marco da Fase 2 registrado.
- **T-017** motor de CI local (instalar→lint→testes→build por projeto, config em
  `_gestao/ci.json`, spawn com timeout e kill de árvore no Windows).
- **T-018** UI de CI/CD (estágios, log ao vivo, editor de config, histórico).
- **T-019** robustez: watchdog de inatividade, guardrails por ação e recuperação
  pós-reinício (jobs órfãos, pendências de input abertas, histórico de CI travado).
- **T-020** polimento e documentação (README do painel reescrito, armadilhas preenchidas,
  estados de carregamento/vazio/erro).

### Revisão crítica a pedido do usuário (achou 4 problemas)
O usuário pediu conferência do que fora construído num modelo mais fraco. Encontrado:
1. **Bug real:** gravação de `_gestao/ci.json` sem garantir a pasta → ENOENT/500 em
   projeto sem `_gestao/`. Pior: o teste original tinha sido "consertado" criando a pasta
   no fixture, **mascarando o bug**.
2. **Regressão de UX:** refetch automático piscava a página inteira e desmontava filhos.
3. **Fragilidade:** `recarregar` recriada a cada render disparava efeitos a cada evento SSE.
4. **Teste quebrado:** o "flaky pré-existente" que eu dispensei a sessão inteira falhava
   em 100% das execuções — usava runner fake de ~2ms e perdia sempre a corrida com o HTTP
   do supertest. Suíte passou a ficar **verde de ponta a ponta pela primeira vez**.

### canUseTool validado (pendência de 6 dias)
`teste:integracao` deixou de ser um `echo` placeholder e virou o teste real. Provado com
o SDK de verdade: pendência criada → job pausa → resposta destrava → fluxo conclui.
**Armadilha:** a 1ª rodada com `echo` saiu INCONCLUSIVA — comando trivialmente seguro é
auto-aprovado antes de chegar ao callback; o gatilho confiável é escrever fora do `cwd`.

## Lições registradas (viraram armadilhas no CLAUDE.md do painel)
- **Corrigir o teste até passar esconde o bug.** Se um teste falha, pergunte primeiro se
  ele está certo e o código errado.
- **Falha que se repete em TODA execução não é flaky.** Rotular sem abrir o teste custou
  horas.
- **Documentação que mente é pior que documentação ausente** — o CLAUDE.md do painel
  afirmava que os jobs não estavam ligados à UI e que havia testes de integração reais.

## Custo real
US$ 0,048 (~R$ 0,27) nas duas rodadas da validação do `canUseTool`, no Haiku — autorizado
pelo usuário. Mais um disparo acidental de `/status` durante um smoke (cancelado antes de
a sessão iniciar; custo ~zero), registrado como erro meu: no painel não existe disparo
"a seco", `POST /api/acoes/:id` já executa.

## Estado / próximos passos
- **painel-fabrica: 20/20 tarefas, 3 fases aprovadas.** Suíte 195/195 (servidor) + 14/14
  (web), build limpo.
- **Pendência que não dá para fechar por aqui:** verificação VISUAL no navegador — não há
  navegador neste ambiente. Marco da Fase 3 foi aprovado com essa ressalva explícita.
- Projetos descartáveis no disco: `teste-todo-cli`, `importado-teste`, `cockpit`.
