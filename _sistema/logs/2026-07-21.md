# Log — 2026-07-21

## Resumo
Primeiro projeto real da fábrica criado: **painel-fabrica** — interface web local
("cockpit") para orquestrar a fábrica pelo navegador (painel por projeto, ações via
botões, análise de código persistida, CI/CD visível, inputs pedidos pela UI). Fluxo
completo do /novo-projeto executado: estrutura + git, pesquisa técnica, especificação,
plano (3 fases) e backlog de 20 tarefas.

## Decisões
- Pesquisa (pesquisas/2026-07-21-claude-code-headless.md do projeto): integração via
  `@anthropic-ai/claude-agent-sdk` (login por assinatura, streaming tipado, canUseTool/
  AskUserQuestion para inputs via UI); CLI headless só como fallback; SSE para a SPA.
- Stack (DECISOES.md do projeto): Express 5 + React/Vite + TS estrito, monorepo npm
  workspaces, sem banco (arquivos da fábrica = fonte única de verdade), Vitest.
- T-001 (spike de autenticação do SDK) é o gate do projeto: se falhar, Fase 2 precisa
  de replanejamento antes de qualquer investimento.

## Estado
- painel-fabrica: 20 tarefas em backlog; T-001 e T-002 promovidas para `pronta`
  (paralelizáveis, areas disjuntas). Fase 1 = painel somente-leitura (T-001–T-006).

## Checkpoints do /trabalhar
- T-001 (spike Agent SDK) — **concluida** (2 ciclos; revisão ciclo 1 pegou caminho de
  plano B documentado errado; auth por assinatura validada com binário empacotado,
  SDK 0.3.217 pinado, alias `fable` funciona headless).
- T-002 (esqueleto monorepo) — **concluida** (2 ciclos; revisão ciclo 1 pegou error
  handler 500 fixo e helper de API descartando headers; corrigidos com testes).
- T-003 (leitor do estado da fábrica) — **concluida** (1 ciclo limpo; 24 testes novos;
  armadilha real do cache do gray-matter descoberta e registrada em DECISOES.md).
- T-004 (API REST de leitura) — **em-teste**, código commitado, suíte verde. Verificação
  formal (testador+revisor) pendente para a retomada.
- T-007 (núcleo de jobs) — **em-teste**, código commitado (robustez de I/O corrigida na
  revisão do ciclo 1), suíte verde. Verificação formal pendente para a retomada.
- Achado: projetos/cockpit/ é casca abandonada de 2026-07-19 (mesma ideia do
  painel-fabrica, sem espec/plano/código) — o painel a exibe com `erros`. Decisão de
  apagar/sanear fica com o usuário.

## Pausa (custo)
Sessão interrompida a pedido do usuário por controle de custo: a leva rodou toda em
**Fable no esforço xhigh** (config mais cara, herdada da sessão via `model: inherit`),
com ~30 execuções de subagente e vários ciclos de retrabalho — gasto alto. Os créditos
do Fable esgotaram no meio da verificação da T-007; sessão migrada para **Opus 4.8**.
Nada ficou rodando. Suíte completa verde ao pausar: servidor 67/67, web 7/7. Correção de
sistema: `argument-hint` de `.claude/commands/trabalhar.md` e `status.md` estava com YAML
inválido (sem aspas) — corrigido na raiz da fábrica.

## Interface de leitura construída direto (pós-pausa, Opus)
O usuário apontou que, apesar do gasto, não havia nada VISÍVEL — só backend. A pedido
dele, construí a UI direto (orquestrador, Opus, sem pipeline de agentes) sobre a API já
pronta:
- **T-005** (home: panorama + 6 ações + projetos) e **T-006** (visão por projeto: kanban
  com detalhe de tarefa, plano com marcos, análise, decisões, progresso) → **concluida**.
- Infra web nova: `web/src/lib/{tipos,formato,useDados}.ts`, `web/src/componentes/`,
  `estilos.css` reescrito (cards, kanban, tiles, cores por status, dark, responsivo).
- Verificação: `npm run build` (tsc estrito + vite, 40 módulos, 0 erro) + smoke test ao
  vivo (servidor 8765 servindo SPA; `/api/fabrica`, `/api/projetos`, `/` 200 com dados
  reais). Testador/revisor formais PULADOS por decisão de custo (registrado nas tarefas).
- **O painel abre e é usável em leitura:** `npm run build && npm start` → 127.0.0.1:8765.
- Commit `af70ed5`.

## Núcleo da Fase 2 construído direto (Opus, sem pipeline)
A pedido do usuário ("faça a próxima fase"), implementei direto o disparo real das ações:
- **T-008** runner do Agent SDK (`servidor/src/jobs/claude/`, SDK pinado 0.3.217 instalado
  no workspace servidor; `query` injetável p/ testes), **T-009** hub + rota SSE
  (`/api/eventos`), **T-011** rotas de ação (`POST /api/acoes/:id`), **T-014** página de
  Jobs com console ao vivo, **T-015** disparo pelos cards da home → todas **concluida**.
- **T-004** e **T-007** fechadas (código verificado por suíte + integração real).
- Guardrails de custo: modelo escolhido no disparo (default econômico `sonnet`, env
  `MODELO_PADRAO`), aviso na ação pesada `/trabalhar`, cancelamento por AbortController.
  permissionMode `bypassPermissions` (ferramenta local; inputs-via-UI = T-010, pendente).
- **Suíte:** servidor 87/87, web 7/7. **Prova real ao vivo:** `POST /api/acoes/status`
  (painel-fabrica, haiku) rodou headless, concluiu em ~63s (~US$0,14, 30 turnos),
  devolveu o painel de status real; 35 eventos capturados no SSE em tempo real.
- Verificação/revisão FORMAIS dispensadas por decisão de custo (registrado nas tarefas e
  em DECISOES.md). Servidor de produção deixado no ar em http://127.0.0.1:8765.

## Mudança estrutural — painel movido para a raiz (ferramenta de sistema)
A pedido do usuário, o painel saiu de `projetos/painel-fabrica/` e virou `<fabrica>/painel/`,
versionado no repositório da RAIZ (junto com `_sistema/`, `.claude/`). Feito:
- 3 processos `tsx` leftover (de execuções anteriores) encerrados — seguravam o node_modules.
- Histórico git do painel (15 commits) preservado num bundle no scratchpad; `.git` aninhado removido.
- `config.ts`: `fabricaRaiz` agora `resolve(raizPainel, "..")` (um nível acima).
- 2 testes de "contrato vivo" ajustados (subiam um `..` a mais). Suíte reverde: 87/87 + 7/7.
- CLAUDE.md da raiz atualizado (mapa + seção Git): painel versionado na raiz, mantido À MÃO
  (fora do pipeline, que só opera em `projetos/`). DECISOES.md do painel registra a mudança.
- Consequências: o painel não se auto-lista mais (só `cockpit` aparece em /api/projetos);
  a fábrica não mantém mais o painel pelo pipeline. Servidor validado no novo local
  (fabricaRaiz = Gerador_de_projetos, SPA + API 200).

## Refino de UX + estratégias de modelo (Fase 2, cont.)
A pedido do usuário (UI confusa; quer opção Fable→Opus não hardcodada; quer custo estimado):
- **Estratégias de modelo data-driven** em `config.ts` (`ESTRATEGIAS_MODELO`): `fable-opus`
  (fallback nativo do SDK — `fallbackModel` é string no `Options` do `query()`), `fable`,
  `opus`, `sonnet`, `haiku`. Disparo agora manda `estrategia` (não `modelo`); a rota resolve
  para modelo+fallback. Runner passa `fallbackModel`.
- **Estimativa de custo** = peso da ação (leve/médio/pesado, em `catalogo-acoes.ts`) × tier
  do modelo (baixo/médio/alto), mostrada ao abrir o disparo; custo real segue vindo do result.
- **UI mais clara/bonita:** home explica o que cada ação faz + selo de peso; formulário de
  disparo com descrição da estratégia e estimativa ao vivo; Jobs com legenda do console.
- Suíte 90/90 + 7/7. Validado ao vivo: `fable-opus` resolve fable+opus, dispara e cancela.

## Pendências da Fase 2 (próxima rodada)
- T-010 inputs pendentes via UI (canUseTool/AskUserQuestion), T-012 ação de análise
  (gera ANALISE.md), T-013 cadastro/importação de projetos, T-016 ações por projeto.
- **Ideia do usuário para /trabalhar:** orquestração genérica que cria sub-agentes SOB
  DEMANDA a partir da ideia do projeto (hoje o pipeline usa os 6 agentes fixos). A estratégia
  de modelo já flui para todos os sub-agentes (herdam o modelo da sessão headless). Próximo
  passo maior: agentes dinâmicos por projeto (Agent SDK aceita `agents` dinâmico) — merece
  design dedicado.
- `projetos/cockpit/` é casca abandonada — decidir apagar/sanear com o usuário.
- Rodar QUALQUER pipeline de agentes num modelo econômico (não Fable/xhigh).
