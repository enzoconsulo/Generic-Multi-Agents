# 2026-08-11

## Retomada: os quatro consertos de 10/08 estavam prontos e não commitados

A sessão anterior terminou com a árvore da fábrica suja — 11 arquivos, 523 linhas, todo o
trabalho da noite de 10/08 em pé mas fora do git. Suíte rodada nesta sessão para confirmar
que estava íntegro: **809 (servidor) + 162 (web), zero falha** (os 796 do log de ontem mais
os 13 testes novos dos consertos). Commitado em `f7d62f6`.

O que entrou (todos vindos de rodada real, não de análise):

1. **Autocalibragem do orçamento media a coisa errada.** `registrarTarefaConcluida` era
   chamada no despacho do revisor com `r.custoUsd` — o custo da ETAPA, não o da TAREFA. Job
   `341ba362`: `custosObservados: [1.68]` contra US$ 7,06 reais na T-034. Subestimativa de
   4×, sempre para baixo, sempre no sentido de começar trabalho que não cabe. E disparava
   também quando o revisor REPROVAVA. Agora o gatilho é o status `concluida` e o número sai
   de `custosPorTarefa`.
2. **Teto POR TAREFA** (`FRACAO_TETO_POR_TAREFA = 0.5`). O teto de job parava limpo e a
   rodada fechava zerada mesmo assim, porque uma tarefa só comia o orçamento em vaivém —
   T-034 duas vezes seguidas (US$ 7,06 e US$ 12,40, nenhuma tarefa bancada). Estacionar não
   é bloquear: status intacto, `tentativas` não gasta, trabalho commitado; sai por
   `emCircuito` e o próximo `/trabalhar` retoma. **Só na fronteira de ciclo** (construtor com
   `tentativas >= 1`) — aplicar em qualquer passo jogaria fora ciclo já pago a um passo de
   fechar.
3. **`conformidade` não é uma coisa só.** `cumpre-parcial` COM achado nomeado agora vira
   escopo pontual com teto de voltas, mantendo o modelo reforçado. A T-034 gastou 34
   chamadas de ferramenta em `opus`, escopo completo e sem teto, para trocar UMA linha de
   CSS.
4. **`captura.mjs --exigir=<expr>`** (repetível, sai 3 se falhar). A metade GEOMÉTRICA de um
   critério de UI é mensurável e sai de graça na passada mecânica; só a estética precisa de
   verificador. A Fase 6 saiu com **zero** `verificar:` em 27 critérios porque "é visual" foi
   lido como "é julgamento". Regra acrescentada ao `planejador`.

## Estado da fábrica na retomada

- **ia-hibrida-limpa:** T-001 a T-006 concluídas. Nada aberto.
- **banco-imobiliario:** árvore limpa, ciclo 2 da T-034 commitado (`897e5b3`).
  - **T-034** `em-teste`, `tentativas: 2` — é o **3º e último ciclo** do protocolo. Se o
    testador reprovar, a tarefa bloqueia e vai para autocorreção (não tem `replanejada-de`,
    então o `planejador` reescreve).
  - **T-039** (PWA lite/manifest) `pronta`, `tentativas: 0`, sem dependência — foi cortada
    pela cota no executor em 16:21 de ontem.
  - T-035, T-036, T-037, T-040, T-041 em `backlog`, todas presas na T-034. T-038 espera
    T-035/T-036/T-042; T-042 espera T-035.
  - **A T-034 é o gargalo de 5 tarefas.** É por isso que ela vale o último ciclo.

**O que o teto por tarefa NÃO vai fazer na próxima rodada, e é de propósito:** T-034 está
`em-teste`, e o próximo passo dela é o verificador, não um construtor. A trava só age na
fronteira de ciclo — ela não vai estacionar uma tarefa a um passo de fechar.

**Dívida de planejamento anotada** (não corrigida aqui, é do planejador): os 4 critérios de
UI da T-034 estão todos `[julgado]` e exigem servidor de pé + rolagem forçada por socket
para serem medidos. Cada ciclo, executor e testador reescrevem o MESMO script descartável
(`_tmp-seed-t034.mjs`) para montar esse cenário — duas contas caras para a mesma pergunta,
e é o que o `--exigir` sozinho não resolve: falta o cenário, não a asserção. Um utilitário
versionado de "sobe servidor + força a jogada" no projeto tornaria a metade geométrica
desses critérios executável de verdade.
