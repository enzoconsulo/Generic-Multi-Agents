# 2026-07-31 — Diagnóstico da parada do /novo-projeto e correções de sistema

## O que houve com o `banco-imobiliario` (job `e6d810ca`, 30/07 19:56–20:08)

O `/novo-projeto` disparado pelo painel **não bateu em cota, não estourou turnos e não foi
cortado pelo watchdog**. Terminou `concluido`, `erro: false`, 18 de 150 turnos, US$ 0,57,
1 sessão, 11 min de execução contra 15 min de tolerância de silêncio. Os 98% da janela
semanal não têm relação — o runner tem disjuntor próprio para cota (`motivo: "limite-uso"`)
e ele não disparou.

**Causa:** o orquestrador despachou o `planejador` em SEGUNDO PLANO e encerrou o turno.
Texto final gravado no resultado: *"serei notificado automaticamente quando o `planejador`
terminar. Vou aguardar essa notificação para validar os artefatos, promover tarefas e
commitar"*. Em job headless (Agent SDK) não existe quem entregue essa notificação: o modelo
parou de emitir, a sessão fechou, o SDK mandou `result` e o job assentou — com o planejador
ainda escrevendo. O mtime do último arquivo criado (`T-013`, 20:08) é o mesmo minuto do
`terminadoEm`.

**Estrago:** `PLANO.md` citando T-001..T-022 com só 13 arquivos em `_gestao/tarefas/`;
nenhuma tarefa promovida; nada commitado (o repo tinha só o commit de estrutura). Do lado
do usuário, o painel mostrava um sucesso.

## Correções (fábrica)

- **CLAUDE.md, regra de ouro 7:** despacho de agente é SÍNCRONO; nunca encerrar turno
  aguardando notificação nem agendar continuação futura.
- **`/novo-projeto`:** despacho síncrono explícito no passo 4, e o passo 5 agora exige
  conferir um a um que todo id citado nas fases do PLANO.md tem arquivo.
- **`/manutencao`:** passou a checar id do plano sem arquivo de tarefa.

## Correções (painel — T-048, mantido à mão)

- **Preâmbulo headless em TODO prompt de job** (`acoes/preambulo.ts`, aplicado às 6 ações,
  às ações por projeto e à análise): a regra chega pelo prompt, não só pelo CLAUDE.md.
- **Detector de despacho em segundo plano** no runner: conta os `run_in_background`, avisa
  no log na hora e no fim, e grava `despachosFundo` no resultado; a aba Jobs mostra
  "Pode ter ficado trabalho pela metade".
- **Histórico de log persistido** (`jobs/historico-log.ts` + `GET /api/jobs/:id/logs`): o
  log de execução vivia só no buffer SSE (500 eventos para a fábrica inteira), então job
  terminado abria sem uma linha — era exatamente o que faltava para explicar esta parada.
  Grava `<id>.log.jsonl` no fim da execução, corta o MEIO acima do teto e some junto na poda.
- **Aviso de planejamento incompleto** na página do projeto: banner com os ids que o plano
  cita e não têm arquivo (antes, só um bloco cinza "T-014?" perdido dentro da fase).
- **Guardrails da ação `analisar` finalmente lidos** — a entrada existia em `guardrails.ts`
  desde a T-019 e `montarJobAnalise` nunca a consultava: a análise subia sem teto de turnos
  e com o watchdog no limite global. Mesma família do `watchdogMs` da T-037.

Suíte do painel verde: 407 (servidor) + 124 (web).

## `banco-imobiliario` — recuperado

`planejador` despachado (síncrono) para completar T-014..T-022: Fase 2 fechou com prisão,
falência, trocas e integração do ciclo de turno; Fase 3 com layout de partida, modais,
painel de propriedades, log/fim de jogo e documentação. 22 arquivos para 22 ids do plano.
T-001 promovida para `pronta` (única sem dependências). Tudo commitado no repo do projeto
(`chore: especificação e backlog inicial`).

Riscos que o planejador registrou e valem acompanhamento: **T-017 é a maior tarefa do
projeto** (resolução de casa, decisões, prisão, falência e fim de jogo num fluxo só) e é a
candidata natural a replanejamento se bloquear; **T-016 e T-017 declaram ambos
`server/index.js`**, então não podem rodar em paralelo.

## Pronto para amanhã

- `/trabalhar banco-imobiliario` começa por T-001 e destrava T-002/T-003/T-004 em paralelo.
- `ia-hibrida-limpa`: sem trabalho pendente (escopo fechado em 30/07).
