TRABALHO PENDENTE DA FÁBRICA — reescrito em 2026-08-16 (madrugada). Este arquivo é
AUTOSSUFICIENTE e é o ÚNICO handoff. Você não precisa de conversa anterior, só dele e dos
arquivos que ele aponta.

Enzo vai dizer só "roda o proximo_prompt.txt". Isso significa: execute os itens NA ORDEM, um
de cada vez, e pare quando o item terminar. Não emende três numa sessão só.

Regra, armadilha e decisão fechada NÃO moram aqui — moram em `CLAUDE.md` (constituição),
`_sistema/DECISOES_FECHADAS.md` (não reabrir sem fato novo) e `painel/CLAUDE.md` (armadilhas
técnicas do cockpit). **Leia `DECISOES_FECHADAS.md` antes de propor qualquer otimização.**

═══════════════════════════════════════════════════════════════════════════════
FECHADO NA MADRUGADA DE 16/08 — não reabrir (commits f0793e5 e 6ff2b94)
═══════════════════════════════════════════════════════════════════════════════

- **ITEM 1 (estouro sem atuador): FECHADO.** Antes de ligar o atuador foi preciso consertar o
  termômetro: medindo as 135 etapas reais de `painel/dados/jobs/*.log.jsonl`, o teto declarado
  estourava em 52% dos verificadores e 36% dos construtores. Alarme que toca em metade das
  rodadas não carrega informação. Agora há dois números: `orcamentoDeFerramentas` é o ALVO do
  prompt; `limiarDeDebate` é o ALARME, no p90 (construtor 65, verificador 37, revisor 28,
  disparo de 6-12%). O atuador é a trava 3 de `politicaDe` — ciclo que se debateu não recebe
  caminho barato no ciclo seguinte. Descoberta colateral: **`areas` não prevê número de
  chamadas** (mediana 21 com ≤2 areas contra 12 com 3), então a escada 30/45/60 escala numa
  variável que não correlaciona.
- **ITEM 2 (portão do meio não independente): FECHADO.** `testador.md`/`conferente.md` exigem
  UMA mutação quando a prova foi criada no mesmo ciclo; `executor.md`/`construtor.md` deixam
  comando copiável de reprodução nas Notas.
- **ITEM 4 (mutex de areas): FECHADO.** `alteracoesForaDe` roda POR ETAPA e escreve nas Notas
  (que é o que o revisor recebe). Não commita: sob paralelismo isso roubaria trabalho alheio.
- **Marco da Fase 2 do banco-imobiliario: APROVADO** por execução real (5 partidas completas
  entre 2 clientes socket.io, 343 rolagens uniformes, animação medida quadro a quadro no
  navegador). Estava `pendente` desde sempre. Fase 3 segue `reprovado` DE PROPÓSITO.

═══════════════════════════════════════════════════════════════════════════════
ITEM 1 (era o 3) — a recuperação por árvore git chega um despacho tarde
═══════════════════════════════════════════════════════════════════════════════
(painel; INVESTIGAÇÃO CONCLUÍDA, falta o conserto — é provavelmente UMA LINHA)

A investigação do handoff anterior está feita. O que se sabe agora:

A recuperação NÃO é de fim de rodada — é por etapa, em `motor.ts` (~linha 1188, dentro de
`if (depois.status === statusAntes)`). O que atrasa é a **assimetria da linha ~1214**:

    vezes >= 2 ? "todos" : "commit"

Na 1ª repetição só o sinal de COMMIT é consultado; o sinal de ÁRVORE SUJA espera a 2ª. Foi
isso que deu à T-035 três construtores seguidos (23:11:03, 23:16:38, 23:19:39 no job
`0345125c`), com o trabalho já no disco desde o primeiro — US$ 2,13 por trabalho pronto.

**A razão escrita para a assimetria não se sustenta neste ponto do fluxo, e é isso que o
conserto precisa checar.** O comentário do parâmetro `sinais` (T-062) justifica o atraso
dizendo que árvore suja é ambígua: "pode ser entrega pronta sem registro (T-025) ou agente
cortado no meio de uma edição". Mas **agente cortado devolve `r.concluiu === false`, e esse
caso é tratado ~250 linhas ANTES** (`if (!r.concluiu)`, ~linha 1034), que tira a tarefa de
circulação. Quando o fluxo chega à recuperação, o agente terminou normalmente e declarou
resultado — a ambiguidade citada não é alcançável ali. O gate de impedimento (~1146), que é o
outro caso legítimo de "terminou sem mexer no status", também já rodou antes.

CONFIRME ISSO NO CÓDIGO antes de mudar (é a única premissa do conserto), e então o conserto é
trocar a linha por `"todos"`. Continua auto-limitado: o commit limpa a árvore, então a
segunda ocorrência não acha trabalho parcial e cai no encerramento por `sem-progresso`.

Escreva teste que falhe antes e passe depois — `motor-retrabalho.test.ts` já tem o mundo
roteirável, com as opções `construtorMudo` e `trabalhoParcial` prontas para exatamente isto.

Armadilha (`painel/CLAUDE.md`): não conserte de um jeito que faça o motor commitar trabalho
que o revisor nunca vai ver. A recuperação atual já faz certo — commita em nome da tarefa e
registra o hash nas Notas, para haver DIFF.

═══
ITEM 2 (era o 5) — o portão do meio é 1-wide e vira o teto de vazão da fábrica madura
═══════════════════════════════════════════════════════════════════════════════
(painel; estrutural, avalie antes de implementar)

`maquina.ts:182`: havendo verificador, ele sai SOZINHO (só com revisores). Construtor vai a
3-wide; verificador é sempre 1-wide, porque a árvore de trabalho é uma só e bateria completa
sobre árvore com edições alheias gera reprovação falsa.

Isso está CERTO hoje. O problema é que a proporção muda com a maturidade do projeto: toda
tarefa passa por 1 construtor, mas por 1 testador + 1 revisor. Numa fase madura o pipeline
deixa de ser limitado por construção e passa a ser limitado por verificação. Medido em
15/08: quatro testadores tiveram de rodar em fila (545 s + 497 s + 233 s + 97 s), e cada
espera é também uma volta do orquestrador — que é o item de 78% da conta, não os 14%.

Saída a AVALIAR (não está em DECISOES_FECHADAS): `git worktree` por verificador. O testador
verifica trabalho COMMITADO, que é exatamente o que um worktree entrega isolado. Pontos a
resolver antes de decidir: onde as evidências (`_gestao/evidencias/`) são gravadas e como
voltam; custo de disco e de `npm install` por worktree; e se o ganho de vazão paga a
complexidade num cockpit local de uma máquina com 7,9 GB. **Traga números antes de
implementar** — pode ser que não pague.

═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
SOBRAS PEQUENAS — todas viraram tarefa em 16/08, NÃO faça à mão
═══════════════════════════════════════════════════════════════════════════════
As três sobras que moravam aqui estão fechadas ou roteadas:

- CLAUDE.md do banco-imobiliario que mente → **T-050** (`pronta`).
- chips a 0,75rem → **T-052** (`backlog`, depende da T-051). O escopo cresceu ao escrever o
  critério: são QUATRO regras abaixo de 0,8rem, duas delas a 0,7rem.
- `server-error.log` → já não existe; árvore limpa.

Abertas junto, do marco da Fase 2 (ver `_sistema/logs/2026-08-16.md`):
- **T-053 (alta)** — teste de broadcast em `tests/turno.test.js` depende de sorte no dado;
  falha rara vira reprovação falsa no portão do meio.
- **T-049 (media)** — `opcoes` de `liquidar-divida` omite `falir`.
- **T-051 (baixa)** — "Sala criada" sai em vermelho, pela função de erro.

Elas são trabalho do PIPELINE, não seu: dispare `/trabalhar banco-imobiliario` pelo painel.
Os cinco comandos `verificar:` foram executados antes de entrar nas tarefas — todos falham
hoje e passam quando o trabalho estiver feito.
