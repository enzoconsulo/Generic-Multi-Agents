Continuar o trabalho no painel-fabrica (o cockpit web da fábrica de software).
O painel vive em `painel/` na RAIZ da fábrica (ferramenta de sistema, versionada no repo da
raiz, mantida À MÃO — fora do pipeline executor/testador/revisor, que só opera em projetos/).

=== SITUE-SE PRIMEIRO (ritual de início de sessão) ===
Leia, nesta ordem:
    1. _sistema/logs/2026-07-22.md        (log mais recente — resume a última sessão)
    2. painel/_gestao/PROGRESSO.md         (histórico do projeto; topo = mais recente)
    3. painel/_gestao/DECISOES.md          (decisões de arquitetura, só-adição)
Depois faça um scan de status:
    Grep '^status:' em painel/_gestao/tarefas/*.md   → 15 concluídas, 5 em backlog (Fase 3).

Como rodar:  cd painel && npm run build && npm start  → http://127.0.0.1:8765
             (ou `npm run dev` para desenvolver com recarga).
Testes:      cd painel && npm test   → hoje 135 no servidor + 7 na web, todos verdes.
             (Testes que exigem login real do Claude ficam em `npm run teste:integracao`.)

=== JÁ FEITO (não refazer) ===
- Leitura completa (home/projetos/kanban/plano), disparo de ações via web com log ao vivo (SSE),
  estratégias de modelo Fable→Opus + estimativa de custo.
- AGENTES DINÂMICOS validados de verdade no Haiku: o /trabalhar despacha por especialista
  (equipe.json → options.agents). Bug corrigido: o trabalhar.md agora escolhe o especialista
  de forma determinística lendo equipe.json (antes tentava adivinhar "se roda pelo painel").
  O painel loga QUAL especialista trabalha (a ferramenta de despacho aqui é `Agent`, não `Task`).
- T-012 (ANÁLISE por projeto): POST /api/acoes/analisar gera/atualiza _gestao/ANALISE.md;
  botão "Analisar/Reanalisar" na página do projeto. Provado ao vivo.
- T-013 (IMPORTAÇÃO pela web): POST /api/projetos/importar copia uma pasta para projetos/,
  ignora node_modules, git init/preserva .git, cria _gestao/ mínimo e enfileira a análise;
  form "Importar pasta existente" na home. Criar projeto novo continua sendo a ação /novo-projeto.
- T-010 (INPUTS pela UI): fluxo pausa em aguardando-input, painel "⏸ Aguardando você" na página
  de Jobs pergunta/aprova; POST /api/inputs/:id/resposta destrava. Autonomia preservada
  (bypass é o padrão; permissionMode:"default" liga as aprovações pela UI).

=== REGRA DE CUSTO (MUITO IMPORTANTE — não esquecer) ===
Já queimei R$550 numa noite rodando o pipeline multi-agente no Fable/xhigh. Então:
- Construa DIRETO você mesmo (orquestrador, no Opus), SEM despachar o pipeline caro de agentes,
  a menos que eu peça.
- Qualquer execução REAL de fluxo da fábrica (planejador, /trabalhar, análise, CI): use modelo
  BARATO (Haiku ou Sonnet), NUNCA Fable/xhigh.
- Antes de qualquer coisa que gaste de verdade, me diga a estimativa de custo.

=== O QUE FALTA (nesta ordem) ===

1) FASE 3 — CI/CD por projeto (é o grande item que resta). Cada tarefa: construa direto,
   teste com build + testes + smoke ao vivo, commite e me apresente ao final. Ordem:

   a) T-017 — MOTOR DE CI local: estágios instalar → lint → testes → build por projeto, como
      jobs NÃO-Claude (usaClaude:false, lock projeto:<nome>), com log ao vivo pelo SSE. Config
      por projeto em _gestao/ci.json (defaults deduzidos do package.json); resultados em
      dados/ci/ do painel. (Ver a decisão "Análise/CI" em painel/_gestao/DECISOES.md 2026-07-21.)
      Dica: espelhe o padrão do runner de importação (T-013) — job não-Claude que roda comandos
      e emite progresso (jobs/inputs e projetos/runner-importar são bons modelos).

   b) T-018 — UI do PIPELINE: aba CI/CD na página do projeto com os estágios, log ao vivo e a
      config (ci.json) editável pela tela.

   c) T-019 — ROBUSTEZ: watchdog, guardrails e recuperação pós-reinício. IMPORTANTE aqui:
      recuperar pendências de INPUT abertas após reinício (a T-010 deixou isso de fora de
      propósito — hoje uma pendência aberta não sobrevive a um restart do processo).

   d) T-020 — POLIMENTO final de UX + documentação (README/CLAUDE.md do painel).

2) VALIDAÇÃO PENDENTE (rápida, quando quiser): a integração REAL do canUseTool (T-010) com o
   SDK nunca rodou paga. Para provar ponta a ponta: dispare um fluxo Claude com
   permissionMode:"default" e um comando fora do allowlist → deve criar uma pendência em
   GET /api/inputs → aprove pela UI/API → o job conclui. Barato no Haiku; me avise o custo antes.
   (A rota de disparo /api/acoes/:id ainda não expõe permissionMode — talvez precise de um
   pequeno ajuste para esse teste, ou faça via um disparo direto.)

=== NOTAS OPERACIONAIS ===
- Projetos de TESTE deixados no disco (descartáveis, pode apagar): projetos/teste-todo-cli
  (tem ANALISE.md gerado — serve de demo) e projetos/importado-teste (demo da importação).
- projetos/cockpit é casca abandonada (com um equipe.json de exemplo) — pode sanear/apagar.
- Pode haver um servidor do painel ainda no ar em http://127.0.0.1:8765 de sessões anteriores;
  se for reiniciar, derrube o processo Node na porta 8765 antes.
- Achado e corrigido nesta sessão: o .gitignore da raiz usava `projetos/` (amplo demais) e
  escondia código/fixtures do painel; agora é `/projetos/` (ancorado). Se criar pastas
  chamadas "projetos" dentro do painel, elas são versionadas normalmente.

Trabalhe com autonomia máxima (decida e registre nos arquivos), respeitando a regra de custo.
