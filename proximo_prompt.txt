TRABALHO PENDENTE DA FÁBRICA — escrito em 2026-08-11, para ser executado quando a cota
estiver folgada. Este arquivo é AUTOSSUFICIENTE: você não precisa de nenhuma conversa
anterior, só dele e dos arquivos que ele aponta.

Enzo vai te dizer só "roda o proximo_prompt.txt". Isso significa: execute os itens abaixo
NA ORDEM, um de cada vez, e pare quando o item terminar (não emende três de uma vez).

Este arquivo é ROTATIVO — ele diz o que fazer AGORA e é reescrito a cada handoff. Regra,
armadilha e decisão fechada NÃO moram aqui (a versão anterior guardava as duas coisas
misturadas, e reescrever o handoff quase apagou conhecimento que custou sessões). Elas
moram em: `CLAUDE.md` (constituição), `_sistema/DECISOES_FECHADAS.md` (o que já foi
respondido e não se reabre sem fato novo) e `painel/CLAUDE.md` (armadilhas técnicas do
cockpit). **Leia `_sistema/DECISOES_FECHADAS.md` antes de propor qualquer otimização.**

═══════════════════════════════════════════════════════════════════════════════
ANTES DE COMEÇAR — três coisas que mudam o resultado
═══════════════════════════════════════════════════════════════════════════════

1. ESTA SESSÃO é o item mais caro da fábrica (78% da conta de 9 dias, contra 14% do
   pipeline inteiro). Faça UM item, entregue, e peça a ele para dar /clear antes do
   próximo. Não tente varrer os três numa sessão só — a conta de uma sessão cresce ao
   quadrado. Detalhe: CLAUDE.md, seção "Disciplina de contexto".

2. Os itens 1 e 2 são no PAINEL, que é mantido À MÃO pelo orquestrador (fica fora do
   pipeline executor/testador/revisor). Você mesmo edita. Rode `cd painel && npm test`
   antes de commitar — a suíte inteira é ~5 min (809 servidor + 162 web na última
   medição, zero falha). Commite na raiz com prefixo `painel:`.

3. O item 3 é uma rodada do pipeline, que Enzo dispara PELO PAINEL. Se em vez disso você
   for despachar agente daqui, passe `run_in_background: false` em TODO despacho e espere
   — segundo plano é o padrão da ferramenta e já destruiu trabalho de agente duas vezes
   nesta fábrica (CLAUDE.md, regra de ouro 7).

ESTADO DA FÁBRICA (2026-08-11, confira antes de agir — pode ter mudado):
- ia-hibrida-limpa: T-001 a T-006 concluídas, nada aberto.
- banco-imobiliario: T-034 concluída. T-035 em-teste (tentativas 1). T-036, T-037, T-039,
  T-040, T-041 e T-043 prontas. T-038 e T-042 em backlog.
- Última rodada: job `0345125c` — 5 despachos, US$ 6,58, T-034 concluída, encerrada pelo
  teto de custo. Leitura completa em `_sistema/logs/2026-08-11.md`.

═══════════════════════════════════════════════════════════════════════════════
ITEM 1 — o orçamento de ferramentas mede e não alimenta decisão nenhuma
═══════════════════════════════════════════════════════════════════════════════
(maior retorno dos três; é análise + código no painel)

O FATO: na rodada `0345125c`, 3 dos 5 despachos estouraram o teto declarado de chamadas de
ferramenta — testador 30 de 25, revisor 28 de 20, executor-reforcado 31 de 30. Nas rodadas
de 10/08 houve estouro de 52 contra 30. Idas ao modelo custam AO QUADRADO, então a cauda de
estouro é onde o dinheiro do pipeline queima.

NÃO "conserte" fazendo o motor cortar o despacho no meio. Isso já foi decidido e a razão
está escrita em `painel/servidor/src/pipeline/despachante.ts`, no comentário de
`orcamentoDeFerramentas` (~linha 56): cortar exigiria converter chamadas em voltas, e
despacho interrompido no meio custa igual sem entregar nada (US$ 4,11 medidos num corte por
cota). É a mesma doutrina do teto de orçamento — nunca cortar no meio, só não COMEÇAR o que
não cabe. Se você reescrever isso, vai estar desfazendo uma decisão sem ler o motivo.

O QUE FALTA DE VERDADE: a medição não alimenta NENHUMA decisão. `estouros` entra no
relatório e morre ali — não influencia escalonamento de modelo, não influencia
replanejamento, não influencia a estimativa do próximo despacho. Um agente que estourou o
teto estava se debatendo, e debater-se REPETE.

O QUE FAZER: investigue e proponha o atuador consistente com a doutrina (agir ANTES de
começar, nunca no meio). Landmarks:
- `painel/servidor/src/pipeline/despachante.ts` — `orcamentoDeFerramentas`, e o ponto onde
  `chamadas`/`orcadoFerramentas` são devolvidos (~linha 349).
- `painel/servidor/src/pipeline/motor.ts` — onde `estouros` é preenchido (~linha 867) e
  onde a política do retrabalho é decidida (a chamada a `politicaDe`).
- `painel/servidor/src/pipeline/diagnostico.ts` — `politicaDe`, que já escolhe modelo,
  `maxTurns` e escopo por natureza da reprovação. É o lugar natural para o sinal entrar.
- `painel/servidor/src/pipeline/orcamento.ts` — `decidir`/`decidirTarefa`, a doutrina de
  "não começar o que não cabe" já implementada; siga a mesma forma (função pura + teste).
Traga a proposta ANTES de implementar se ela mudar comportamento de despacho.

═══════════════════════════════════════════════════════════════════════════════
ITEM 2 — por que a recuperação por árvore git não pegou na T-035
═══════════════════════════════════════════════════════════════════════════════
(investigação; provavelmente conserto pequeno)

O FATO: na rodada `0345125c`, a T-035 recebeu TRÊS despachos de construtor seguidos, sem
verificador entre eles (executor sonnet, executor sonnet, executor-reforcado opus). As Notas
de execução do terceiro dizem, com todas as letras: "a sessão anterior implementou o CSS/JS
na árvore de trabalho, sem commit, e encerrou ANTES de gerar a evidência e commitar" — e ele
aproveitou 100% do trabalho. Custo dos dois primeiros despachos: US$ 2,13, por trabalho que
ficou no disco sem ninguém registrar.

POR QUE ISSO DEVERIA TER SIDO PEGO: existe uma recuperação que pergunta à árvore git se
houve trabalho quando o construtor termina sem gravar status — dois sinais, HEAD ter andado
e mudança não commitada nas `areas`. As `areas` da T-035 são exatamente
`public/js/tabuleiro.js` e `public/css/tabuleiro.css`, que eram os arquivos sujos. Ela
deveria ter commitado em nome da tarefa e promovido a em-teste no PRIMEIRO despacho.

LANDMARKS:
- `painel/servidor/src/pipeline/motor.ts`, a partir da ~linha 1160 (os comentários longos
  sobre recuperação de trabalho não registrado — leia-os, contam dois casos reais).
- Dados brutos da rodada: `painel/dados/jobs/0345125c.json` e `0345125c.log.jsonl`.
- A tarefa: `projetos/banco-imobiliario/_gestao/tarefas/T-035-tabuleiro-mobile-fit.md`.
- `MAX_DESPACHOS_POR_TAREFA` vale 12 (motor.ts ~299), então o teto de vaivém não agiu nem
  perto — o freio esperado era a recuperação, não ele.

Cuidado com a armadilha registrada em `painel/CLAUDE.md`: "antes de desistir de um agente,
pergunte se ele produziu alguma coisa" — e a inversa, que é o caso aqui. Não conserte de um
jeito que faça o motor commitar trabalho que o revisor nunca vai ver.

═══════════════════════════════════════════════════════════════════════════════
ITEM 3 — T-043 e depois o backfill dos `verificar:` da Fase 6
═══════════════════════════════════════════════════════════════════════════════
(é rodada de pipeline; Enzo dispara pelo painel)

CONTEXTO: os 27 critérios de aceite da Fase 6 do banco-imobiliario não têm NENHUM
`verificar:`, então caem todos em `[julgado]` e o testador refaz do zero o ritual que o
executor acabou de fazer (subir servidor, entrar na sala, forçar jogada por socket, dirigir
o navegador). Duas contas caras para a mesma pergunta.

O backfill foi TENTADO em 11/08 e abortado de propósito. Não repita as duas formas óbvias:
1. A passada mecânica (`painel/servidor/src/pipeline/criterios.ts`) recusa comando com
   `< > ; | & $` ou crase. Afirmação geométrica é feita de `<=`/`>=`: todo `--exigir` é
   rejeitado como `encadeamento-proibido` ANTES de rodar.
2. `_sistema/ferramentas/captura.mjs` NÃO sobe servidor. `verificar:` apontado para
   localhost bate em porta morta na passada mecânica → reprovação falsa, um ciclo perdido.
Decisão completa: `projetos/banco-imobiliario/_gestao/DECISOES.md`, entrada de 2026-08-11.

SEQUÊNCIA:
a) Rodar a T-043 (`_gestao/tarefas/T-043-cenario-verificavel.md`) — ela cria
   `ferramentas/cenario.mjs`, com cenas nomeadas que sobem o servidor, montam o estado,
   capturam e afirmam, e é chamada por `node ferramentas/cenario.mjs --cena=<nome>`: um
   binário, zero metacaractere. Está `pronta`, prioridade alta, sem dependências, `areas`
   disjunta das outras — pode rodar em paralelo com T-036/T-037/T-040/T-041.
b) DEPOIS que ela concluir e as cenas forem reais, escrever os `verificar:` de T-036,
   T-037, T-040 e T-041 apontando para as cenas que existem de fato. Não invente nome de
   cena: rode `node ferramentas/cenario.mjs --listar` e use o que ele imprimir.
c) VALIDE todo `verificar:` que escrever contra as regras reais antes de commitar — a
   regex `PERIGOSO` e a `BINARIOS_PERMITIDOS` estão em `criterios.ts` (~linhas 60-98).
   Comando "quase certo" custa um ciclo inteiro (a T-030 deste projeto custou 4 ciclos e
   US$ 12,90 assim). E nunca escreva `verificar:` para um comando que DEVE falhar: a
   passada mecânica lê qualquer saída != 0 como critério reprovado.

═══════════════════════════════════════════════════════════════════════════════
SOBRAS PEQUENAS (faça de passagem, não gaste sessão com isso)
═══════════════════════════════════════════════════════════════════════════════
- `projetos/banco-imobiliario/server-error.log` (0 bytes) ficou não rastreado — algum agente
  subiu servidor redirecionando stderr e não limpou. Apagar ou pôr no .gitignore do projeto.
- T-039 (PWA lite/manifest) está `pronta` há dois dias e sem dependência nenhuma; foi
  cortada pela cota em 10/08. É a tarefa mais barata do backlog — boa para fechar rodada.
