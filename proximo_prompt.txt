Continuar o trabalho na fábrica Gerador_de_projetos.

=== SITUE-SE PRIMEIRO (ritual de início) ===
Leia, nesta ordem:
    1. _sistema/logs/2026-07-27.md      (a sessão anterior INTEIRA — comece por aqui)
    2. painel/_gestao/PROGRESSO.md       (histórico do painel; topo = mais recente)
    3. painel/CLAUDE.md                  (seção "Armadilhas conhecidas" — 13 armadilhas
                                          reais, cada uma de um bug que já custou tempo)
Scan de status (um Grep resolve):
    ^status: em painel/_gestao/tarefas/*.md          → 27/27 concluida
    ^status: em projetos/*/_gestao/tarefas/*.md      → ia-hibrida-limpa: 1 pronta, 5 backlog

Como rodar:  duplo-clique em INICIAR.bat (raiz) → abre http://127.0.0.1:8765 sozinho.
             Ou: cd painel && npm run build && npm start
Testes:      cd painel && npm test  → 209 (servidor) + 33 (web), todos verdes.
             ATENÇÃO: FECHE o painel antes de testar. Painel no ar compete por recursos e
             derruba o worker do vitest com ERR_IPC_CHANNEL_CLOSED — parece bug, não é.
Teste pago:  npm run teste:integracao (~US$0,01, valida canUseTool contra o SDK real).


=================================================================
JÁ ESTÁ FEITO — NÃO REFAZER
=================================================================

O PAINEL (T-001..T-023) ESTÁ COMPLETO, Fases 1-3 com marco. MAS ver o item 1:
ficou faltando a página de Jobs visual (T-024, criada e pronta para executar).
  - Leitura: panorama, projetos, kanban, plano, decisões, progresso, análise.
  - Execução: as 6 ações da fábrica pela web, escolha de modelo, estimativa de custo,
    log ao vivo (SSE), cancelamento, inputs pela UI (canUseTool VALIDADO em execução paga).
  - Importação de pasta com SELETOR NATIVO de pasta (botão "Escolher…", roda no backend
    porque o navegador não entrega caminho absoluto).
  - CI local por projeto, genérico por ecossistema (Node, Python, Go, Rust, .NET, Maven,
    Gradle) — stack não reconhecida recebe config em branco editável, não erro.
  - Robustez: watchdog de inatividade, guardrails de turnos por ação, recuperação
    pós-reinício (jobs órfãos, pendências de input, histórico de CI travado).
  - Visualização: equipe ao vivo (quem trabalha agora), mapa do planejamento (fases com
    progresso, tarefas como blocos clicáveis) e página /como-funciona.
  - Repositório no GitHub (enzoconsulo/Generic-Multi-Agents), clonável, com INICIAR.bat.

GENERICIDADE — JÁ PROVADA, não precisa auditar de novo:
  O planejador criou equipe coerente para PYTHON/STREAMLIT no ia-hibrida-limpa
  (`ia-integracao` + `streamlit-ui`). Antes disso só havia prova num CLI Node.


=================================================================
O QUE FAZER — EM ORDEM
=================================================================

1) VOCÊ CONSEGUE VER A TELA — use isso (novidade de 2026-07-28)
   `cd painel && npm start` e depois:
     node ferramentas/captura.mjs http://127.0.0.1:8765/projeto/<nome> saida.png --espera=3000
   Dirige o Edge/Chrome já instalado via DevTools Protocol, sem instalar nada, e o PNG
   pode ser LIDO com a ferramenta Read. Foi o que destravou a maior lacuna do projeto:
   até 27/07 nenhuma tela tinha sido vista renderizada.
   NÃO use `--screenshot` direto do navegador (captura antes do React pintar) nem
   `--virtual-time-budget` (derruba o navegador nesta máquina — testado).
   Derrube o painel antes de rodar `npm test`: painel no ar quebra o worker do vitest.

   >>> A partir de agora, tarefa de UI só vai para `concluida` DEPOIS de capturar e olhar.
   Já rendeu 7 defeitos numa única passada (T-027), sendo dois deles INFORMAÇÃO FALSA na
   tela com a suíte inteira verde.

   Para provar comportamento ao vivo (cronômetro, pulso, progresso) SEM gastar assinatura:
   crie um projeto temporário em projetos/ com um `_gestao/ci.json` cujo estágio rode um
   script lento (`node -e "setInterval(...)"`), dispare POST /api/ci/<nome>/rodar e capture
   duas vezes com intervalo. Job de CI é gratuito — roda processo local, não usa Claude.
   Apague o projeto de teste ao terminar.

2) EXECUTAR O ia-hibrida-limpa (é o passo natural; nada de código do painel envolvido)
   Estado: ESPECIFICACAO + PLANO (3 fases) + T-001..T-006 criados; equipe com
   `ia-integracao` e `streamlit-ui`. T-001 `pronta`, resto em `backlog` aguardando a cadeia.
   Feature: geração completa de anúncio (título + descrição orientados a venda, no contexto
   da foto) na aba de criar imagens do estudio_shopee.

   Como: painel → projeto ia-hibrida-limpa → "Trabalhar neste projeto".
   CUSTO: é o fluxo mais caro que existe (pipeline completo). USE HAIKU OU SONNET.
   NUNCA Fable/xhigh. Avise a estimativa ao usuário ANTES de disparar.

   O que observar (é a primeira execução multi-agente de ponta a ponta num projeto real
   de stack não-Node): se os especialistas certos são despachados, se o testador roda os
   critérios de verdade, e se a tela "equipe ao vivo" reflete quem está trabalhando.

3) VERIFICAÇÃO VISUAL NO NAVEGADOR (a única pendência que sobreviveu ao projeto)
   Nenhuma tela do painel foi vista renderizada — o ambiente do Claude não tem navegador.
   Toda a lógica é testada, o VISUAL não. Peça ao usuário para olhar, ou verifique você se
   tiver navegador disponível. Vale conferir: aba CI/CD, equipe ao vivo, mapa do plano,
   estado vazio da home, e o seletor de pasta (se a janela abre NA FRENTE do navegador).

4) PENDÊNCIAS CONHECIDAS DO ia-hibrida-limpa (não são bugs do painel)
   a) `Local_AI/` é um repositório git ANINHADO dentro do projeto. O `git add` registrou
      só uma referência quebrada: o conteúdo não entra no histórico e um clone vem vazio
      ali. Decidir com o usuário: apagar `Local_AI/.git` (vira código normal do projeto)
      ou transformar em submódulo de verdade.
   b) O CI não vai detectar ecossistema: o `requirements.txt` está em
      `Local_AI/analista_dados_shopee/`, e a detecção só olha a RAIZ do projeto. Isso
      degrada bem (config em branco editável pela UI), mas se incomodar, a melhoria é
      procurar marcador em subpastas — ver `painel/servidor/src/ci/ecossistemas.ts`.

5) MELHORIAS DO PAINEL JÁ MAPEADAS (só se o usuário pedir)
   - Home também poderia ter o "próximo passo sugerido" (hoje só a página do projeto tem).
   - Histórico de execuções por agente ao longo do tempo (hoje a equipe ao vivo mostra só
     o job atual).
   - O log do job não sobrevive à sessão: `dados/` guarda metadados, não o stdout. Se for
     necessário rever log de execução antiga, precisa persistir as linhas.


=================================================================
REGRAS QUE NÃO PODEM SER ESQUECIDAS
=================================================================

CUSTO: já foram queimados R$550 numa noite em Fable/xhigh.
  - Construa DIRETO você mesmo (orquestrador), SEM despachar o pipeline caro de agentes,
    a menos que o usuário peça.
  - Execução real de fluxo (planejador, /trabalhar, análise, CI): Haiku ou Sonnet.
  - SEMPRE diga a estimativa ANTES de gastar. E lembre: no painel não existe disparo "a
    seco" — `POST /api/acoes/:id` já executa de verdade.

DISCIPLINA DE VERIFICAÇÃO (as três lições que mais custaram tempo nesta sessão):
  - Corrigir o teste até passar ESCONDE o bug. Se um teste falha, pergunte primeiro se ele
    está certo e o código errado. (Um ENOENT/500 real ficou mascarado assim.)
  - Falha que se repete em TODA execução NÃO é flaky. Abra o teste antes de rotular.
  - Documentação que mente é pior que documentação ausente — a próxima sessão decide em
    cima dela.

O PAINEL É MANTIDO À MÃO pelo orquestrador: fica fora do pipeline
executor/testador/revisor (que só opera em projetos/). Mudou o painel? Rode
`cd painel && npm test` antes de commitar, e commite na raiz da fábrica.

Trabalhe com autonomia máxima (decida e registre nos arquivos), respeitando a regra de custo.
